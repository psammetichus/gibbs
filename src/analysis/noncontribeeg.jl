#taken from Estimating the likelihood of epilepsy from clinically noncontributory electroencephalograms using computational analysis: A retrospective, multisite case–control study by Tait et al. 2024
#
#


function estNoncontribEEG(eeg)

    #calc peak α freq at O1 and O2 
    #calc high α power = relative α (10.5-13.5) averaged across channels after
    #bipolar referencing frontal and temporal channels
    #
    #calc low α (6-9) phase locking value as adjacency matrix of undirected
    #graph
    #calc mean degree
    #calc degree variance
    #calc avg weighted clustering coeff
    #calc characteristic path length
    #
    #calc global coupling
    #calc local coupling
    #calc critical coupling

end
